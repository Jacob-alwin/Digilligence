import { useState, useEffect } from "react";

// Usage
export default function Size() {
  useEffect(() => {
    window.addEventListener("resize", () => {
      if(window.innerWidth>=)
    });
  }, []);
}

export const { isLap } = Size();
